extends base.pug

block content 
  h1 Editing: #{videoSelected.title}
  form(method="POST")
    h2 Change the title of the video
    input(name="title", type="text", value=`${videoSelected.title}` required) 
    input(type="submit", value="save")